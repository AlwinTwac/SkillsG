specVersion: "v1"
serviceId: "skillsg"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "skillsg-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
